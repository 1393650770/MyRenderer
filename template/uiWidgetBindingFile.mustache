#pragma once
// Auto-generated by MetaParser. Do not edit.
// UIWidget BindingTraits for classes with UI_BIND_* annotations.

#include <RmlUi/Core/DataModelHandle.h>
#include "UI/Widget/UIWidgetBinding.h"
{{#include_headfiles}}
#include "{{headfile_name}}"
{{/include_headfiles}}

namespace MXRender {
namespace UI {
namespace Widget {

{{#class_defines}}
template<>
struct UIWidgetBindingTraits<{{namespace_name}}{{class_name}}>
{
	static void BindDataModel(void* ctor_opaque, {{class_name}}* data)
	{
		auto& ctor = *static_cast<Rml::DataModelConstructor*>(ctor_opaque);
	{{#fields}}
		ctor.Bind("{{{display_name}}}", &data->{{field_name}});
	{{/fields}}
	{{#twoway_fields}}
		ctor.Bind("{{{display_name}}}", &data->{{field_name}});
	{{/twoway_fields}}
	{{#actions}}
		ctor.BindEventCallback("{{{display_name}}}",
			[data](Rml::DataModelHandle handle, Rml::Event& event, const Rml::VariantList& /*arguments*/) {
				data->{{method_name}}(handle, event);
			});
	{{/actions}}
	}

	static Vector<UIBindingEntry> GetBindingEntries(const rttr::type& type)
	{
		return {
	{{#fields}}
			{ "{{{display_name}}}", type.get_property("{{field_name}}"), EBindingMode::OneWay, {} },
	{{/fields}}
	{{#twoway_fields}}
			{ "{{{display_name}}}", type.get_property("{{field_name}}"), EBindingMode::TwoWay, {} },
	{{/twoway_fields}}
		};
	}

	static Map<String, Map<String, String>> GetFieldMetadata()
	{
		return {};
	}
};

{{/class_defines}}
} // namespace Widget
} // namespace UI
} // namespace MXRender
